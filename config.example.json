{
  "_comment": "Schema reference for repi config. The real file lives at .repi/config.json and is written by `repi init` or the web UI. NEVER commit a populated copy of this file — it contains API keys.",
  "REPI_ENV": "production",
  "LOG_LEVEL": "INFO",
  "DATABASE_URL": "postgresql+asyncpg://repi_user:password_here@localhost:5432/repi",
  "REDIS_URL": "redis://localhost:6379",
  "ENABLE_REDIS_CACHE": true,
  "LLM_PROVIDER": "anthropic",
  "LLM_MODEL": null,
  "LLM_API_KEY": "REPLACE_ME",
  "OLLAMA_BASE_URL": "http://localhost:11434",
  "TIME_WINDOW_INITIAL_MINUTES": 10,
  "TIME_WINDOW_EXPANSIONS": "60,360,1440",
  "UI_PORT": 3000,
  "WATCHER_CONFIG_REFRESH_SECS": 30
}
