.git
.github
.venv
.worktrees
.pytest_cache
.repi
.impact
.env
.env.*
__pycache__
**/__pycache__
*.pyc
*.pyo
*.pyd
.DS_Store

# Source we don't need in the image
tests/
eval/
docs/
scripts/
utils/
lograg/

# web/ IS shipped — only exclude its build/install artifacts so the web-builder
# stage gets a clean install + build.
web/node_modules
web/.next
web/.env.local

# Local artifacts
bug.json
bug_baseline.json

# Docs/meta the runtime doesn't need.
# (README.md stays — hatchling references it during the package build.)
CLAUDE.md
LICENSE
Makefile
docker-compose.yml
Dockerfile.*
